# CLAUDE.md — docs 作用域

本目录只放开发过程文档，不是运行时配置目录。

## 约束

- `docs/reports/` 只放工程报告
- `docs/plans/` 只放实施计划与流程设计
- 文档要能解释“为什么这样做”，不只罗列现象
- 发布、安装、治理决策若变更，必须先在这里补足过程文档与工程报告
- 涉及安装、升级、日常使用、排障观察的说明，必须给出实际 `ccclaw` 命令与判读路径
- 架构切换点如 `daemon/tmux`、`stream-json`、`sevolver` 收敛逻辑变更，必须在报告中写清旧语义与新语义差异
