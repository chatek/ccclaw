# 260310_NA_main_first_revision_convention

## 背景

本次需要将 `issue10-json-tmux-phase1-2` 合并回 `main`，同时把仓库日常修订的主线优先策略固化到治理文件，减少不必要的短生命书签与分叉。

## 处理

- 基于 `main` 与 `issue10-json-tmux-phase1-2` 创建 `jj` 合并工作副本，准备将 Issue #10 Phase 1/2 实现正式并回主线。
- 在 `AGENTS.md` 追加约定：除非绝对必要，否则优先直接在 `main` 上修订；仅在需要隔离风险、并行评审或分阶段交付时再新开书签或分支。

## 结果

- 主线修订策略从口头约定转为仓库级显式规则，后续执行体在是否开新书签时有统一判断依据。
- `issue10-json-tmux-phase1-2` 的回并与治理规则补充可在同一次合并提交中完成，避免再次制造额外分叉。

## 验证

- 待合并提交完成后，执行 `jj log` 确认 `main` 指向新的合并结果。
- 执行相关测试确认合并后工作树可用。
